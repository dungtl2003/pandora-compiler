#### `E0006`: UnterminatedCharLiteral

A character literal wasn't ended with a quote.

Erroneous code example:
```
set c:char = 'c; // error!
```

To fix this error, add the missing quote:

```
set c:char = 'c'; // ok!
```
