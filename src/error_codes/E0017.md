#### `E0017`: unclosed delimiter

An opening delimiter is not closed.

Erroneous code example:

```
{ ( [ // error!
```

To fix this error, ensure that each opening delimiter is closed with the correct closing delimiter. 

Example:

```
{} () [] // ok!
```
