add std;
add os::path;

fun main() {
    var a: int = 5;
    var b: float = 6;
    var c: float; 
    c = a as float + b;
    print_hello(c as str);
}

fun print_hello(s: str) -> void {
    print("{s}");
}
