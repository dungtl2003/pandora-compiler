pub fun add<T ext Plus::<T>>(self, a34: T, b: T) -> void {
    var a:int;
    var mut b:int;
    if 1 < 2 {if 3 < 4 {}} else if 5 < 6 {}
    while a<b {
        for c in d {}
    }
    3>2;;;;;;;;;;
    return 87+4;;;;;;;;
}