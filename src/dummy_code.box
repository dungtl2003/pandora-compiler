/// This is main function.
fun main() {
    let name: String = String.new("John Cena");
    let age: number = 21; // Calculate my age.

    print_raw_message(name, age);
}

/*@ This is another function.
fun print_raw_message(name: String, age: number) {
    sysout("Hello ${name}, you are ${number} years old!\n");
}
